export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DANGER = 'danger',
  SUCCESS = 'success',
  LIGHT = 'light',
  WARNING = 'warning',
  INFO = 'info'
}

export enum IconPosition {
  LEFT = 'left',
  RIGHT = 'right'
}

export enum TextPosition {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center'
}
